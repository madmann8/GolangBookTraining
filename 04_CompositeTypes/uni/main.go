package main

import "fmt"

func main() {
	v := []byte("v")
	fmt.Println(v)
	/*fmt.Printf("%v",squash(v))
}

func squash(b []byte) []int{
	var k []int
	for i,n:= range b{
		if b[i]==b[i+1]{
			k=append(k,n)
		}
		i++
	}
	return k
}
*/
	}